# Property file with JDBC settings.

jdbc.driverClassName=com.mysql.jdbc.Driver
# Immediately after the connection to database is set up, we switch to the mybatis_test database.
jdbc.url=jdbc:mysql://localhost:3306/mybatis
jdbc.username=root
jdbc.password=password
